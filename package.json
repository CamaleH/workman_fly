{
  "name": "workman_fly",
  "scripts": {
    "build": "vite build",
    "clean": "rm -rf dist/*",
    "clean-build": "npm run clean && npm run build",
    "dev": "vite"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vite": "^7.0.0"
  }
}
